<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail (name,image, order_id, setmeal_id,dish_id, dish_flavor, number, amount) values
        <foreach collection="orderDetailList" item="item" separator=",">
            (#{item.name},#{item.image},#{item.orderId},  #{item.setmealId},#{item.dishId}, #{item.dishFlavor}, #{item.number}, #{item.amount})
        </foreach>
    </insert>
    <select id="getSalesTop10" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name as name, sum(od.number) as number from order_detail od left outer join orders o on od.order_id = o.id
        <where>
            <if test="begin">
                and o.order_time &gt; #{begin}
            </if>
            <if test="end">
                and o.order_time &lt; #{end}
            </if>
            <if test="status">
                and o.status = #{status}
            </if>
        </where>
        group by od.name order by count(name) desc limit 10
    </select>
</mapper>