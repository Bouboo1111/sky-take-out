<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders (user_id,number, pay_method, pay_status, amount, remark,phone,address, consignee,
                            address_book_id, status, cancel_reason, rejection_reason,checkout_time,
                            cancel_time, estimated_delivery_time, delivery_status,
                            delivery_time, tableware_number, tableware_status, pack_amount, order_time)
        values (#{userId},#{number},#{payMethod},#{payStatus},#{amount},
                #{remark},#{phone},#{address},#{consignee},#{addressBookId},#{status},#{cancelReason},
                #{rejectionReason},#{checkoutTime},#{cancelTime},#{estimatedDeliveryTime},#{deliveryStatus},
                #{deliveryTime},#{tablewareNumber},#{tablewareStatus},#{packAmount},#{orderTime})
    </insert>

    <update id="update" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' "> cancel_reason=#{cancelReason}, </if>
            <if test="rejectionReason != null and rejectionReason!='' "> rejection_reason=#{rejectionReason}, </if>
            <if test="cancelTime != null"> cancel_time=#{cancelTime}, </if>
            <if test="payStatus != null"> pay_status=#{payStatus}, </if>
            <if test="payMethod != null"> pay_method=#{payMethod}, </if>
            <if test="checkoutTime != null"> checkout_time=#{checkoutTime}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="deliveryTime != null"> delivery_time = #{deliveryTime} </if>
        </set>
        where id = #{id}
    </update>

    <select id="pageQuery" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="number != null and number != ''">
                and number=#{number}
            </if>
            <if test="status != null and status != ''">
                and status= #{status}
            </if>
            <if test="phone != null and phone != ''">
                and phone like '%${phone}%'
            </if>
            <if test="beginTime != null and beginTime != ''">
                and order_time == #{beginTime}
            </if>
            <if test="endTime != null and endTime != ''">
                and order_time == #{endTime}
            </if>
        </where>
    </select>
</mapper>